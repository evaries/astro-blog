---
const { title, description, className } = Astro.props;
---

<div class:list={["flex items-center justify-center py-3 bg-bgColor", className || ""]}>
	<div class="flex max-w-[400px] flex-col">
		<span class="text-center text-xl font-bold">{title}</span>
		<p class="block text-center">
			{description}
		</p>
	</div>
</div>
